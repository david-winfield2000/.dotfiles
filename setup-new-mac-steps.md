Install homebrew
Configure home-brew (add to path)
Clone my .dotfiles from GitHub
Bootstrap my .dotfiles
Clone my neovim config
brew bundle
brew link ffmpeg-full imagemagick-full --force --overwrite
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

possibly have a work configuration that sets up:

- awsmyid
- claude code
- uses my disney git config instead of my personal one
